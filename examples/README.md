# Examples

Small CSV datasets for local upload and route verification:

- `sample_sales.csv` should route as `sales`.
- `sample_inventory.csv` should route as `generic`.
- `sample_support.csv` should route as `generic`.
